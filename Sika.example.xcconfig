// Sika.example.xcconfig
// Template for Sika.xcconfig. Copy to Sika.xcconfig and fill in real values.
// Sika.xcconfig is gitignored; this template IS committed.
//
// Note: $() between https: and / escapes the // (xcconfig comment delimiter).
SUPABASE_URL = https:/$()/your-project.supabase.co
SUPABASE_ANON_KEY = your-anon-key-here
